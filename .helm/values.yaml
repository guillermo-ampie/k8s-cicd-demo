namespace:
   name: demo

replica:
   count: 3

labels:
   app: nginx
   tag: alpine

image:
  repository: nginx
  tag: alpine
  pullPolicy: IfNotPresent

resources: 
  cpu: 50m
  memory: 256Mi

service:
   type: ClusterIP
   port: 8111
   targetPort: 80

configmap:
   data: "version: alpine"
